# gcp-deployment-aereal.org

Deployment configuration for aereal.org

```
# gcloud auth application-default login
gcloud deployment-manager deployments update aereal-org --config ./aereal-org.yaml
```
